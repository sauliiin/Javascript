const nome = 'Saulo'
const nom1 = "Mestre"
const nom2 = `Yoda`
let nomeAluno // funfa e recebe mesmo sem colocar o igual, mas recebe o valor undefined. Pra const não funfa isso
let sobrenomeAluno = null // Null (nulo) serve pra quando a gente quer desconfigurar uma variável e colocar ela como nula. null é diferente de undefined.
const boolean = true // Só pode ser true or false. É um valor lógico!